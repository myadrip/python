//https://leetcode.com/problems/reverse-string/

        for i in range(len(s)//2):
            temp = s[i]
            s[i] = s[-1-i]
            s[-1-i] = temp
        
===============================================================
s = s.reverse()

===============================================================
s[:] = s[::-1]
